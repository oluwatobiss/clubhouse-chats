<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/form.css" />
  <link rel="stylesheet" href="/index.css" />
  <title><%= title %></title>
</head>

<body>
  <main>
    <nav>
      <div class="logo">ClubHouse Chats</div>
      <div class="nav-btns-container">
        <%- userStatus ? '<a href="/log-out" class="log-out-btn">Log out</a>' : "" %>
        <%- userStatus === 'author' ? '<a href="/join-club" class="join-club-btn">Join club</a>' : "" %>
        <a href="/" class="return-home-btn">Back to home</a>
      </div>
    </nav>
    <article>
      <h1>Create new post</h1>
      <% if (locals.errors) {%>
      <ul>
        <% errors.forEach(function(error) { %>
        <li><%= error.msg %></li>
        <% }) %>
      </ul>
      <% } %>
      <form action="/new-post" method="post">
        <label for="title">Title</label>
        <input type="text" name="title" id="title" value="<%= errInputs.title %>" required>
        <label for="text">Write a post</label>
        <textarea name="text" id="text" placeholder="What is good?!" required><%= errInputs.text %></textarea>
        <button type="submit">Post</button>
      </form>
    </article>
  </main>
</body>

</html>